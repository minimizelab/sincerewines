import React, { FunctionComponent } from 'react';

const Text: FunctionComponent = ({ children }) => (
  <span className="font-sans text-base text-sincere-green">{children}</span>
);

export default Text;
